apply plugin: 'maven'

repositories {
  jcenter()
  flatDir{
      dirs 'libs'
  }
  maven { url 'dependencies' }
  mavenCentral()
}

dependencies {
   compile fileTree(dir: 'libs', include: ['*.jar'])
   compile(name:'android-sdk-5.2.0', ext:'aar')
}

android { }
