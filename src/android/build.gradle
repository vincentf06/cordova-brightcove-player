apply plugin: 'maven'

repositories {
  jcenter()
  flatDir{
      dirs 'libs'
  }
  maven { url 'dependencies' }
  mavenCentral()
}

dependencies {
   compile fileTree(dir: 'libs', include: ['*.jar'])
   compile(name:'android-sdk-6.2.3', ext:'aar')
}

android { }
