apply plugin: 'maven'

repositories {
  jcenter()
  flatDir{
      dirs 'aar/com/brightcove/sdk/'
  }
  maven { url 'dependencies' }
  mavenCentral()
}

dependencies {
   compile fileTree(dir: 'libs', include: ['*.jar'])
   compile(name:'android-sdk-6.2.3', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
