repositories {
    google()
    jcenter()
    flatDir {
        dirs '../app/libs'
    }
}

dependencies {
   api fileTree(dir: 'libs', include: ['*.jar, *.aar'])
   api(name:'android-sdk-5.2.0', ext:'aar')
}
